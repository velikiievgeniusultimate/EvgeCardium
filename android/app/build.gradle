plugins { id 'com.android.application' }

android { namespace 'game.vibecards.loader'; compileSdk 35
    defaultConfig { applicationId 'game.vibecards.loader'; minSdk 26; targetSdk 35; versionCode 1; versionName '1.0' }
    sourceSets { main.assets.srcDirs = ['../../game'] }
}
